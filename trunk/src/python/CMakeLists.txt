
# CHANGE NAME OF SWIG INCLUDE FILE HERE.
SET_SOURCE_FILES_PROPERTIES(pyscovil.i PROPERTIES CPLUSPLUS ON)
SET_SOURCE_FILES_PROPERTIES(pyscovil.i PROPERTIES SWIG_FLAGS "-includeall")

SWIG_ADD_MODULE(pyscovil python pyscovil.i)

# CHANGE NAME OF PYTHON MODULE HERE.
SWIG_LINK_LIBRARIES(pyscovil
   scovil ${PYTHON_LIBRARIES}
   )